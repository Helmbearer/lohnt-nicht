{
  "name": "lohnt-nicht",
  "version": "0.0.1",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist"
  },
  "dependencies": {
    "vue": "^3.0.0"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^1.2.0",
    "vite": "^2.0.0",
    "gh-pages": "^3.2.3"
  }
}
